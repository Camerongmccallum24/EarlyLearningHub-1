# Environment Variables for DigitalOcean App Platform Deployment
# Copy these to your App Platform environment variables configuration

# Application Configuration
NODE_ENV=production
PORT=8080

# Database Configuration (DigitalOcean Managed Database)
DATABASE_URL=postgresql://username:password@host:port/database_name
# Alternative database URL (for backward compatibility)
DATABASE_URL_ATS=postgresql://username:password@host:port/database_name

# PostgreSQL Connection Details (auto-populated by DigitalOcean)
PGHOST=your-db-host
PGPORT=25060
PGUSER=your-db-user
PGPASSWORD=your-db-password
PGDATABASE=your-db-name

# API Keys and External Services
OPENAI_API_KEY=your_openai_api_key_here

# Security
SESSION_SECRET=your_very_secure_session_secret_here

# Optional: Redis Configuration (if using Redis for sessions)
REDIS_URL=redis://username:password@host:port